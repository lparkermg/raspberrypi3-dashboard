define(['libs/knockout'],
  function BluetoothScannerViewModel(ko){
    return function(){
      var self = this;

      //This will house the functions that activate the scanner
      //Along with displaying the returned items from the scan.
    };
  });
