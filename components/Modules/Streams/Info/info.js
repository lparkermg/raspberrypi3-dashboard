define(['libs/knockout'],
  function StreamsInfoViewModel(ko){
    return function(){
      var self = this;

      //This will house the functions for the following.
      //- Getting information on things like how long the stream has been playing, if a stream is playing, etc.
    };

  });
